/*class temperature { //A PEAUFINER
  void draw() {
  
  float v = map(pico.getA(), 0, 1000, 0, 30);
   if (pico.getA() > 450) {
    //tint(255, 0+v);
  image(chaleur, 0, 0, width, height);
   }
   if (pico.getA() < 90) {
   tint(255, 5+v);
   image(froid, 0, 0, width, height);
   }
}
}*/
